<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:util="clr-namespace:Please2.Util"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:command="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP8">
    
    <Style x:Key="FuelLabel" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    
    <DataTemplate x:Key="BioDiesel">
        <Border Background="White" Padding="10" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="104"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0">
                    <Glyphs FontRenderingEmSize="" FontUri="" UnicodeString=""/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="12,0,0,0">
                    <TextBlock Text="{Binding station_name}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                    
                    <TextBlock Text="{Binding address}"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Usage: " Style="{StaticResource FuelLabel}"/>
                        <TextBlock Text="{Binding groups_with_access_code}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Usage: " Style="{StaticResource FuelLabel}"/>
                        <TextBlock Text="{Binding groups_with_access_code}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="Electric">
        
    </DataTemplate>
    
    <DataTemplate x:Key="Ethanol">
        
    </DataTemplate>
    
    <DataTemplate x:Key="Hydrogen">
        
    </DataTemplate>
    
    <DataTemplate x:Key="CNG">
        
    </DataTemplate>
    
    <DataTemplate x:Key="LNG">
        
    </DataTemplate>
    
    <DataTemplate x:Key="LPG">
        
    </DataTemplate>

    <DataTemplate x:Key="fuel">
        <util:FuelTemplateSelector Content="{Binding}"
                                   BioDiesel="{StaticResource BioDiesel}"
                                   Electric="{StaticResource Electric}"
                                   Ethanol="{StaticResource Ethanol}"
                                   Hydrogen="{StaticResource Hydrogen}"
                                   CompressedNaturalGas="{StaticResource CNG}"
                                   LiquifiedNaturalGas="{StaticResource LNG}"
                                   LiquifiedPetroleumGas="{StaticResource LPG}"
                                   HorizontalContentAlignment="Stretch"/>
    </DataTemplate>

    <DataTemplate x:Key="shopping">
        <Border Background="White" Padding="10" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="104"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0">
                    <Image Source="{Binding image}" Width="104" Height="104"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="12,0,0,0">
                    <HyperlinkButton x:Name="ShoppingTitle" 
                                         NavigateUri="{Binding url}" 
                                         TargetName="shopping" 
                                         Content="{Binding title}"
                                         Style="{StaticResource CustomHyperlinkButtonStyle}"
                                         Foreground="Blue" 
                                         Margin="-12,0,0,0" 
                                         HorizontalAlignment="Left"/>
                    <TextBlock x:Name="ShoppingPrice" 
                                   Text="{Binding price}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   FontSize="{StaticResource PhoneFontSizeLarge}" 
                                   FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="events">
        <Border Background="White" Padding="10" Margin="0,15,0,0">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Tap">
                    <command:EventToCommand Command="{Binding EventItemSelection}" CommandParameter="{Binding}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="104"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0">
                    <Image Source="{Binding image}" MaxWidth="100"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="12,0,0,0">
                    <TextBlock x:Name="EventTitle" 
                                   Grid.Column="1" 
                                   Text="{Binding title}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap" 
                                   FontWeight="SemiBold"/>

                    <TextBlock x:Name="EventTime" 
                                   Grid.Column="1" 
                                   Text="{Binding start_time, Converter={StaticResource PrettyDateConverter}}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black"/>

                    <TextBlock x:Name="EventLocation" 
                                   Grid.Column="1" 
                                   Text="{Binding location.address}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="movies">
        <Border Background="White" Padding="15">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Tap">
                    <command:EventToCommand Command="{Binding MovieItemSelection}" CommandParameter="{Binding}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <Image Source="{Binding image}" MaxWidth="100" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="MovieTitle" 
                                   Grid.Column="1" 
                                   Text="{Binding title}" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap" 
                                   FontWeight="SemiBold"/>

                    <TextBlock x:Name="MovieRating" 
                                   Grid.Column="1" 
                                   Text="{Binding mpaa_rating}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black"/>

                    <TextBlock x:Name="MovieDescription" 
                                   Grid.Column="1" 
                                   Text="{Binding critics_consensus}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="news">
        <Border Tag="{Binding url}" Background="White" Padding="15">
            <Grid>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="NewsTitle" 
                                   Text="{Binding title}" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap" 
                                   FontWeight="SemiBold"/>

                    <TextBlock x:Name="NewsSource" 
                                   Text="{Binding source}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black"/>

                    <TextBlock x:Name="NewsDate" 
                                   Text="{Binding date}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap"/>

                    <TextBlock x:Name="NewsSummary" 
                                   Text="{Binding summary}" 
                                   TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="real_estate">
        <Border Background="White" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <Image Source="{Binding image}" MaxWidth="100" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="RealEstateTitle" 
                                   Grid.Column="1" 
                                   Text="{Binding title}" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap" 
                                   FontWeight="SemiBold"/>

                    <TextBlock x:Name="RealEstateLocation" 
                                   Grid.Column="1" 
                                   Text="{Binding location.name}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black"/>

                    <TextBlock x:Name="RealEstateSource" 
                                   Grid.Column="1" 
                                   Text="{Binding source.name}" 
                                   Margin="0,10,0,0" 
                                   Foreground="Black" 
                                   TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="flights">
        <Border Background="{StaticResource ResultItemBackground}" BorderThickness="2" BorderBrush="#51575c" Padding="20" Margin="0,10,0,0">
            <Grid>
                <StackPanel>
                    <TextBlock Text="{Binding identification}" FontWeight="SemiBold"/>

                    <Border BorderThickness="1" BorderBrush="White" HorizontalAlignment="Stretch"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding status}"/>
                        <TextBlock Text="{Binding delay}"/>
                    </StackPanel>

                    <TextBlock Text="Departed:" FontWeight="SemiBold"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding origin.city}"/>
                        <TextBlock Text="{Binding origin.airport_code, StringFormat=' ({0})'}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <StackPanel x:Name="DepartedScheduled" Grid.Column="0">
                            <TextBlock Text="Scheduled:" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding schedule.filed_departure}"/>
                        </StackPanel>

                        <StackPanel x:Name="DepartedActual" Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="Actual:"/>
                            <TextBlock Text="{Binding schedule.actual_departure}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Border BorderThickness="1" BorderBrush="White"/>

                    <TextBlock Text="Arrives:"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding destination.city}"/>
                        <TextBlock Text="{Binding destination.airport_code, StringFormat=' ({0})'}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <StackPanel x:Name="ArrivesScheduled" Grid.Column="0">
                            <TextBlock Text="Scheduled:"/>
                            <TextBlock Text="{Binding schedule.filed_departure}"/>
                        </StackPanel>

                        <StackPanel x:Name="ArrivesActual" Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="Estimated:"/>
                            <TextBlock Text="{Binding schedule.estimated_arrival}"/>
                        </StackPanel>

                    </StackPanel>
                </StackPanel>
            </Grid> 
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="contacts">
        <Border Tag="{Binding url}" Background="White" Padding="15" Tap="Contact_Tap">

            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="NewsTitle" 
                               Text="{Binding DisplayName}" 
                               Foreground="Black" 
                               TextWrapping="Wrap" 
                               FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="choice">
        <Border Tag="{Binding url}" Background="White" Padding="15" Tap="Contact_Tap">
            <TextBlock Text="{Binding text}"/>
        </Border>
    </DataTemplate>
</ResourceDictionary>